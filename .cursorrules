Always use TypeScript and React Three Fiber. For Computer Vision, prioritize MediaPipe Tasks Vision. When mapping landmarks to the biped_robot.glb, reference bone rotations using quaternion slerping rather than direct position mapping.

You may run git commit, git add, and git push when asked. Never modify git config (user.name, user.email). Use the existing git identity — the user will appear as the author. You may run read-only git commands (e.g. git status, git log, git diff) when helpful.
