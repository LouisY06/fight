// =============================================================================
// riggedMechPackConstants.ts — Constants for the mech pack
// Now uses procedural code-based models instead of GLB.
// =============================================================================

/** Legacy GLB URL — kept for reference but no longer loaded. */
export const RIGGED_MECHS_PACK_GLB_URL = '/assets/characters/rigged_mechs_pack_2.glb';

/** Number of selectable mechs in the pack (5 procedural designs). */
export const MECH_PACK_COUNT = 5;
